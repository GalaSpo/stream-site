Drop game-day photos in this folder, then add each filename to
`gallery-images.js` at the repo root. That's the whole process — no other
files need to change.

Keep photos reasonably sized (under ~500KB each) so the page loads fast on
a phone.
